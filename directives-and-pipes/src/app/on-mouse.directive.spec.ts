import { OnMouseDirective } from './on-mouse.directive';

describe('OnMouseDirective', () => {
  it('should create an instance', () => {
    const directive = new OnMouseDirective();
    expect(directive).toBeTruthy();
  });
});
